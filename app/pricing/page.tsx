import Link from 'next/link'

const plans=[['Free','$0','App discovery, search, favorites, public bundles and security information.'],['Pro','Coming soon','Advanced installer automation, update policies, multiple PCs and richer collections.'],['Business','Coming soon','Centralized device management, organization policies, deployment reporting and team controls.']]
export default function PricingPage(){return <main className="section"><div className="container"><div className="pill">Pricing</div><h1>Simple software management</h1><p className="lead">Start with the core AppNest experience for free. Paid automation features will only be enabled when the underlying service is production-ready.</p><div className="grid">{plans.map(([name,price,text])=><div className="card" key={name}><h2>{name}</h2><div style={{fontSize:30,fontWeight:800,margin:'12px 0'}}>{price}</div><p className="muted">{text}</p><Link className="btn secondary" href={name==='Business'?'/business':'/apps'}>{name==='Business'?'Business details':'Get started'}</Link></div>)}</div></div></main>}
