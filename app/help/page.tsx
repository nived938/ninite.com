import Link from 'next/link'

export default function HelpPage(){return <main className="section"><div className="container narrow"><div className="pill">Help center</div><h1>How AppNest works</h1><div className="faq"><div className="card"><h3>Are downloads hosted by AppNest?</h3><p className="muted">Not by default. Until a release is verified with a checksum and signature, AppNest sends you to the publisher's official source. This prevents fake or invented download URLs.</p></div><div className="card"><h3>Why does an app say release pending verification?</h3><p className="muted">The catalog can contain publisher metadata before a release has completed the verification workflow. Direct AppNest installer generation is blocked until the release is verified.</p></div><div className="card"><h3>How do I build a setup?</h3><p className="muted">Select apps in the directory, choose Create Installer, review the selection and generate a signed installer configuration.</p></div><div className="card"><h3>Where are installed apps detected?</h3><p className="muted">The web dashboard shows inventory reported by the optional Windows client. The client is designed to report versions rather than upload personal files.</p></div></div><div style={{marginTop:20}}><Link className="btn secondary" href="/security">Read the security model</Link></div></div></main>}
