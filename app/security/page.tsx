export default function Security(){return <main className="section"><div className="container"><div className="pill">Trust center</div><h1 style={{fontSize:46}}>Software security, made visible.</h1><p className="muted" style={{fontSize:18,maxWidth:720}}>AppNest separates publisher metadata from verified installer metadata. A release is not marked verified until an administrator records the source, checksum and available digital-signature evidence.</p><div className="grid" style={{marginTop:28}}>{[['Official sources','Prefer publisher-owned download and release pages.'],['Checksums','SHA-256 values are stored per version and architecture.'],['Signatures','Authenticode or equivalent signature evidence can be recorded when available.'],['Transparent status','Every application page shows whether a release is verified or still pending.']].map(([a,b])=><div className="card" key={a}><h3>{a}</h3><p className="muted">{b}</p></div>)}</div><section className="section"><div className="card"><h2>How sourcing works</h2><p className="muted">Catalog entries are database records, not scraped claims. Administrators can attach official publisher URLs, version metadata, hashes, signatures and verification notes. The installer API rejects unverified releases and never accepts arbitrary client-provided executable URLs.</p></div></section></div></main>}
