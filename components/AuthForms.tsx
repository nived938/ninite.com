'use client'
import {useState} from 'react'
import {signIn} from 'next-auth/react'
import {useRouter} from 'next/navigation'
export function LoginForm(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const x=await signIn('credentials',{email,password,redirect:false});if(x?.error)setError('Invalid email or password.');else r.push('/dashboard')}return <form className="card" onSubmit={submit}><h2>Sign in</h2><input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} style={{width:'100%',padding:12,margin:'8px 0',border:'1px solid var(--line)',borderRadius:8}}/><input required type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} style={{width:'100%',padding:12,margin:'8px 0 14px',border:'1px solid var(--line)',borderRadius:8}}/><button className="btn primary" style={{width:'100%'}}>Sign in</button>{error&&<p className="error">{error}</p>}<button type="button" className="btn ghost" style={{width:'100%',marginTop:8}} onClick={()=>signIn('github',{callbackUrl:'/dashboard'})}>Continue with GitHub</button><button type="button" className="btn ghost" style={{width:'100%',marginTop:8}} onClick={()=>signIn('google',{callbackUrl:'/dashboard'})}>Continue with Google</button></form>}
export function RegisterForm(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const x=await fetch('/api/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,email,password})});const d=await x.json();if(!x.ok){setError(d.error);return}await signIn('credentials',{email,password,callbackUrl:'/dashboard'})}return <form className="card" onSubmit={submit}><h2>Create account</h2><input required placeholder="Name" value={name} onChange={e=>setName(e.target.value)} style={{width:'100%',padding:12,margin:'8px 0',border:'1px solid var(--line)',borderRadius:8}}/><input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} style={{width:'100%',padding:12,margin:'8px 0',border:'1px solid var(--line)',borderRadius:8}}/><input required minLength={10} type="password" placeholder="Password, 10+ characters" value={password} onChange={e=>setPassword(e.target.value)} style={{width:'100%',padding:12,margin:'8px 0 14px',border:'1px solid var(--line)',borderRadius:8}}/><button className="btn primary" style={{width:'100%'}}>Create account</button>{error&&<p className="error">{error}</p>}</form>}
